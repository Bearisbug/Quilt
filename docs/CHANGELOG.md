# Quilt 变更记录（归档）

`docs/DESIGN.md` 与 `docs/TEST.md` 的变更记录只留最近几版，更早的按原文搬到这里，格式与来源表一致、内容不改。当前状态以两份文档正文为准，本文件只回答「某条规则是哪一版、为什么定下来的」。

## DESIGN.md 变更记录（v0.4–v0.54）

| 日期 | 版本 | 改动 | 作者 |
| --- | --- | --- | --- |
| 2026-09-22 | v0.54 | **组件交互契约**（`REQ-EDIT-006` 补充，§13 增「组件交互契约」一节）：组件此前只会产出写死选中态的静态 HTML——用户明确要求「点击任意 Tab 切换 active、indicator 移动」，模型也照着写了，但 `validateComponentHtml` 硬禁 `<script>`（组件 HTML 会被复制进每一屏，脚本进去就是 N 份），于是交上来的只能是把「推荐」高亮焊死的一版，退回重写也只是再来一版静态的。改法不是放开脚本，而是给组件**自己的交互契约**并写进 `componentSystemPrompt`：① 区分「指向别的屏」的导航选中（照旧 `aria-current` 由屏派生）与「同屏内切换」的组件自身状态；② 后者一律 CSS-only——隐藏原生 radio/checkbox + `peer-checked:` 变体，`<label>` 整块可点，默认值写在 `checked` 上，`name` 带组件名前缀防抢；③ 条目用重复块 + `data-slot` / `data-part` 表达，禁 `nth-child` 定制与写死条目数。实测这套在真实组件预览文档里成立（Tailwind Play CDN 运行时生成 `peer-checked:` 变体，预览运行时只劫持 `<a href>` / `[data-href]`，不吃 `<label>` 的点击）。`validateComponentHtml` 不变，`TC-EDIT-012` 增第 10 步 | @bug |
| 2026-09-22 | v0.53 | **画布位置刷新即在原处、组件卡可交互、e2e 认库闸**（`PAGE-CANVAS` 前端行为 + 测试脚手架，`REQ-CORE-005` / `REQ-EDIT-006` 补充）：① **画布视图位置按项目留存**（`quilt:view:<projectId>`，`INT-019`）。此前每次刷新都是「先画在初始位置 `{80,80,0.5}`，再用 300 ms 动画适配到内容」——用户看到的是镜头自己动一下。现在惰性初值读一次、首帧即终值（世界层的 transform 直接写在 render 上，不等 panzoom 的 effect，那个 effect 跑在绘制之后），存过镜头就**一次适配都不做**；平移缩放防抖 300 ms 落盘，无痕模式写不了就退化为只本次会话有效。取值按当前数据校验（缩放不在 [0.1, 2] 或存的是 NaN 一律当没存过），重置入口是工具栏既有的「适配视图 F」。换项目时 `CanvasView` 带 `key={projectId}` 重挂，不把上一个项目的镜头带过去。② **共享组件卡可交互**：与屏一致（双击进、`Esc` 出、与屏聚焦互斥），交互态摘掉手势罩把指针交给 iframe，角标「交互中 · <组件名>」。**但镜头不动**——组件卡本就按自身内容尺寸 1:1 渲染，没有屏那种「推到 1:1 居中」的理由，为看一眼组件把整块画布推走反而丢上下文。③ **e2e 认库闸**（`tests/e2e/lib.ts` 的 `assertTestApi()`，挂在 `launch()` / `apiJson()` / `connectMcp()` 三个入口）：动手前问 `GET /v1/health` 的新字段 `database`（只回库名，不回连接串），库名不含 `_test` 就当场停并给出另起一套 API 的原话；连不上也停（失败即关）。`pnpm mcp:call` 是手工开发脚本，显式 `ALLOW_E2E_DEV=1` 放行。起因不是假设：2026-09-21 并行会话把 e2e 打到 3100 的开发实例上，浏览器与 MCP 的逐屏写删绕过了 `pnpm seed` 那道守卫，开发库里四个项目的屏被清空。`TC-CORE-007` 增第 7 步、`TC-EDIT-012` 增第 11 步 | @bug |
| 2026-09-22 | v0.52 | **一键排成一行 / 排成一列**（`REQ-CORE-018` 补充）：排列条原有的「等距」只在首尾两屏之间均分间隙，选中的屏本来叠着或散着时排完还是叠着或散着，要先手动拉开首尾再等距、再对齐，三步才能得到一排能看的屏。排列条增第三组两个键「排成一行」「排成一列」（≥ 2 屏即可用）：按固定间距 80 px（与造屏落位 `layoutNewScreens` 同一间距）把选中的屏排成同一行（同 y）或同一列（同 x），顺序取当前位置（行按 x 升序、列按 y 升序，相同再按另一轴），起点取选中集合外接框左上角，组不跳走；只动位置变了的屏，落库、失败口径与撤销同对齐 / 等距，撤销标签「排列」。不做「按内容自适应间距」也不做可配置间距：这是「先摆整齐再看」的一键动作，要精确摆放仍用拖动与对齐。`TC-CORE-034` 增第 8b～8d 步 | @bug |
| 2026-09-21 | v0.51 | **MCP 与画布同面**（`REQ-AGENT-002` 补充，`API-AGENT-002` 工具表重列）：此前 MCP 只铺了「agent 自带 HTML 推屏」一条主路——读端接近全覆盖，写端缺一大截：建错的屏删不掉；写坏的屏回不去（`list_revisions` 只列不取，没有回溯、没有采用候选）；新屏一律接在最右且挪不动；素材推不上去；原型导不出；作业取消不了；造 / 改屏选不了通道、带不了参考图、补不了断链屏；九种作业只开放两种。这一版把画布能做的每一件事都投影成工具（22 → 54 个），只有通道的增删改（含密钥）留在设置页：删屏 / 删组件 / 删项目；读旧修订 HTML、回溯、逐屏与整组采用候选、列候选；`move_screens` 一次摆一组屏与组件（与 v0.47 批量移动同一条 PATCH 路径）；`generate_screens` / `edit_screens` 补齐 `anchor` / 懒生成 `route + name + fromScreenId` / `runner` / `attachmentIds` / `componentIds`（参考图经 `create_attachment_upload_url` 签名直传，通道 id 经 `list_runners` 取）；`regenerate_subtree` / `edit_component` / `propose_design_system` / `export_prototype`（+ `get_export` 给下载地址）四种作业；`list_jobs` / `cancel_job` / `get_job_events`；素材 `list_assets` / `create_asset`（读服务端本机文件路径——MCP 与 Quilt 同机是本地版前提）/ `delete_asset`；预设 `list_design_presets` / `create_design_preset` / `apply_design_preset` / `delete_design_preset`；批注 `list_annotations` / `update_annotation` / `delete_annotation` / `send_annotations`（不提供建批注：气泡位置要在画布里量）；`edit_element` 零 token 直改；`list_messages`。顺手修四处工效：`update_screen` 的 `name` / `route` 改可选（缺省沿用当前值），`update_project` 收 `exemplarScreenId`，`update_design_system` 收 `conventions`（提炼作业的产出由 agent 自己确认后写回），`create_project` 收 `presetId`。工具数翻倍对聊天助手意味着每轮多带约 10 K token 的工具说明，暂不裁剪、记入 §28。`TC-AGENT-011` 新增 | @bug |
| 2026-09-21 | v0.50 | **输入框工具条几何：不再互相挤压，宽度按内容自适应，切模式有过渡且高度不抖**（`PAGE-CANVAS` 前端几何，无接口变化）。三件事：① 工具条左组去掉 `min-w-0`——它能缩到 0 而里面的控件全是 `shrink-0`，于是盒子缩了、内容原样溢出去压在右组上（实测通道名长一点时「加参考图」压在屏数档位上 21×32 px，屏数的「1」整档点不到）。② 输入框宽度改为**按工具条量出来**：Composer 在 `useLayoutEffect` 里把工具条临时设成 `max-content` 读一次一行排满的宽度（与外壳当前多宽无关，不会来回振荡），加上外壳内边距写进 `--composer-need`，CSS 取 `min(max(30rem, need), 可用宽)`；造态实测 709 px（工具条右缘余量 0），聊天态收到 30rem 下限 480 px。③ 宽度与横向落点走 180 ms `--ease-out` 过渡，**工具条在过渡期间不折行**（`flex-nowrap` + 外壳 `overflow:hidden`）——否则宽度动画中途工具条会先折成两行再弹回一行，高度来回跳，那就是「抖」；只有可用宽度真的装不下终态时才打上 `data-bar="wrap"` 允许折行，这时裁剪不会藏住任何东西。折行判据读画布的可用区探针，不能去 parse `--chrome-left`（自定义属性取回来是没求值的 `calc(...)`，`parseFloat` 得 NaN，判断会静默恒假、窄视口下工具条被裁掉够不到）。首帧不做过渡（`data-measured`），否则每次开页面都先长一下。`TC-CORE-023` 增第 12 步 | @bug |
| 2026-09-21 | v0.49 | **预览响应不再声明 immutable**（`API-CORE-016` 修订）：v0.48 的滚动条修复推上去之后，已经开着的画布里那条滚动条还在——`withCurrentRuntime` 让这份响应每次下发都不一样，而它带的却是 `private, max-age=600, immutable`，浏览器在 10 分钟里连条件请求都不发；iframe 又是聚焦那一刻才建的，刷新页面（含硬刷新）只覆盖本次导航拉取的资源，绕不过这层缓存。于是「运行时修复不必等每张屏再出一版修订」（v0.34 的承诺）实际最长要等 10 分钟。改成 `Cache-Control: no-cache`，`/p/` 与 `/c/` 同改（组件预览文档同样是每次现拼的）；本地形态下重取一份 30 KB HTML 没有成本，预览域也随之不再走 CDN。`TC-CORE-010` 增第 3c 步 | @bug |
| 2026-09-21 | v0.48 | **预览里不再露出滚动条**（`ADR-003` 补充）：卡片里是一台设备，桌面浏览器却在它右缘画一条常驻经典滚动条（macOS 接鼠标即如此，实测槽宽 15 px）——那条线会被当成设计的一部分，还把 390 px 的屏挤成 375 px 可用宽。预览运行时进文档时注入 `*{scrollbar-width:none}*::-webkit-scrollbar{display:none}`：只隐藏滚动条像素，滚动能力全留（屏内 `overflow-y:auto` 的区域同理，所以用 `*`）。走运行时而不是 prelude，老修订下发时也去掉（`withCurrentRuntime`）；导出产物是在桌面浏览器里当真页面播放的，不注入。`TC-CORE-010` 增第 3b 步 | @bug |
| 2026-09-21 | v0.47 | **多选批量移动**（`REQ-CORE-004` 补充）：此前多选之后按住任一张卡片拖动，按下那一刻就把选择收成这一张、只有它跟着走，框选出来的一组要一张张挪。现在拖动集合按「按下的卡片在不在选中集合里」定：在，选中的屏与共享组件整组等量位移（相对位置逐像素保持）；不在，只拖它自己。按在已选中的卡片上不再一按就换选择，松手没拖过才收成只选它。松手后逐张走既有位置 PATCH，等全部有结果再决定（口径同排列：失败的点名、成功的不回滚）；位置撤销栈同时记组件，`⌘Z` 整组还原（v0.46 的组件拖动此前不进栈）。`TC-CORE-007` 增第 4～6 步 | @bug |
| 2026-09-20 | v0.46 | **共享组件**（新增 `REQ-EDIT-006`、`ADR-019`）：导航栏这类跨屏共用的东西此前每屏由模型重写一遍，会漂移——组件配方只有 10 条一行开标签、只在建项目时写入、无处可改；约定是文字规则、样板屏是照着重写，都不是「复制」。现在共享组件是项目级的一段 HTML、画布上的一等对象（与屏、风格指南卡并列，可拖可框选可当目标）；屏**引用**组件、不复制代码：屏里只放一个占位根元素 `<nav data-component="TabBar"></nav>`，每次写入屏时在注入阶段由 `expandComponents` 确定性展开成正式 HTML（lint / 大纲 / 截图 / 导出 / `get_screen` 看到的都是展开后的 DOM），改组件一次 → 所有用它的屏零 LLM 同步（新修订 `source_kind=component`，与 token 回刷同一条路）。上下文分级：每个组件永远进稳定前缀一张卡（名字 + 一行摘要 + 占位写法，约 30 token），完整 HTML 只在框选了它或目标屏本来就用它时附上。参数只两种：导航型的激活项按屏路由自动算（`activeClass` / `inactiveClass` + `aria-current`）、随屏变的文字用 `data-slot`。新实体 `ENT-Component`（表 `components` + 派生表 `component_uses`，迁移 `0014`）；新接口 `API-EDIT-004`（提取 / 直建 / 改 / 删，改完同步回刷）；`API-CORE-006` 增 `edit_component` 作业与 `generate` / `edit_screens` 的 `componentIds`；`API-CORE-010` 增 `targetComponentIds`；`API-EDIT-001` 增 `detach` 操作与 `409 /errors/component-locked`；MCP 增 `quilt.create_component` / `quilt.update_component`，契约带 `sharedComponents`；预览域增 `/c/{projectId}/{componentId}`；检查器「记为共享组件」/「改组件」/「脱离共享」；工具栏「新建组件」；§16 真值表加行；§28 v0.31「同类屏漂移」标注由本版覆盖导航型 | @bug |
| 2026-09-20 | v0.45 | **聊天模式**（新增 `REQ-CORE-023`、`ADR-018`）：输入框第三种动词「聊」，与造 / 改并列——一句话交给住在 Quilt 进程里的助手，范围由它定（只回答、改一屏、改几屏、改设计系统都由它判断），记得这个项目里前几轮说过什么，能只聊不改。起因：对照 Claude Design 的「一份 HTML + 对话框」，Quilt 差的是跨轮指代与只聊不改这两件；但它那个产物模型（15 个页面塞进一份 2.27 MB、约 57 万 token 的文档，每句话都带着整本书）正是聊满上下文的根因——借它的对话体验，不借它的产物模型。实现：助手 = Claude Agent SDK 的回路（`query()` 挂 Quilt 自己的 MCP，`maxTurns` 放开，内置文件 / shell 工具关掉），循环、会话记忆、上下文压缩全是借来的，Quilt 只补薄的一层：新作业类型 `chat`（`API-CORE-006`；`API-CORE-010` 增 `mode="chat"`；项目级同时只跑一个，索引同 `generate`）、`projects.chat_session_id` 记 SDK 会话按轮 `resume`（迁移 `0013`）、新 MCP 工具 `quilt.get_outline`（每屏一份确定性 DOM 结构摘要，助手先看目录再决定打开哪张）、进度事件 `progress{stage:"chat", step}` 把它正在读 / 改哪张屏推到输入框上方。上下文按 `ADR-012` 的思路分层：稳定前缀（规则 → DESIGN.md → 简介 → 屏注册表）+ 项目大纲 + SDK 会话里的对话记忆，整屏 HTML 只在助手用工具读时才进上下文，一轮 15–25K token、与屏数无关。**只对「本机 Claude 订阅」（`agent-sdk`）通道可用**：Agent SDK 只认 Claude；聊天模式下通道下拉只列它们，一条都没有时就地说明去设置页加。 | @bug |
| 2026-09-20 | v0.44 | **字体来源**（`REQ-EDIT-003` 扩写）：设计系统的字体从 7 个 Google 字体的白名单改为「族名 + 来源」——`google`（任意 Google Fonts 族名，prelude 照旧发 fonts.googleapis 链接）、`system`（本机 / 系统字体如 SF Pro、PingFang SC，不发任何外链，回退栈 `-apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC", system-ui`）、`url`（任意 https 样式表链接：自托管或第三方 CDN 的 `@font-face` CSS）。起因：对照 Stitch 的产出，它的屏随手用 Space Grotesk + Inter 与 Material Symbols 图标字体，而 Quilt 连白名单外的 Google 字体都选不了，模仿 iOS 的屏更是永远用不上 SF Pro / PingFang。`tokens.typography` 增 `fontSource` / `fontUrl`（旧行缺省按 `google` 读，不回填）；`API-EDIT-002` 与 `quilt.update_design_system` 增同名字段，来源为 `url` 而没给链接 → 400；预设表加 `font_source` / `font_url`（迁移 `0012`）。族名与链接会进 HTML 属性、CSS 与 JS 字符串三种上下文，服务端只收 `^-?[\p{L}\p{N}][\p{L}\p{N} _-]{0,79}$` 的族名与不含引号 / 尖括号的 https 链接。`FONT_FAMILIES` 降级为面板的候选提示。 | @bug |
| 2026-09-20 | v0.43 | **设计契约从闸门改成透镜**（`ADR-005` 重大修订，Owner 2026-09-20 决定）：Quilt 是完全的设计工具,用来做**下一版**设计,而不是给已有 app 出对照稿。用当前设计系统的词汇表设计不出下一版设计系统——探索新配色、新字阶、新形状,恰恰要求写得出那 26 个 token 以外的东西;只允许表达现有契约的工具是模板填充器,不是设计工具。改动:① **写入永不因 lint 被拒**——MCP 推屏、元素直改、生成作业三条路径都只记录不阻断,`/errors/lint-failed` 退役;② lint 结果降级为**逐屏的偏离报告**(存在 `lintReport` 里、画布与 MCP 都可见),写明代价「这屏有 N 处硬编码颜色,换主题不会跟着变」,让取舍是知情的而不是靠禁止回避;③ 生成不再为 lint 跑修复回合、不再以 `errorClass=lint` 失败;④ token 与 `designMd` 在 prompt 里从「禁止越界」改为「共享词汇表,越界要自己承担不随主题变的代价」;⑤ **允许外部开源库**——屏内可用 `<script>` 与 CDN(Chart.js、自写组件库等),预览域与导出产物的 CSP 相应放开到 `https:`。 |
| 2026-09-20 | v0.42 | **MCP 补两个写入口**（`REQ-AGENT-002` 扩写）：接 Ofcourt 真实项目时暴露的阻塞——本机 agent 按截图与源码手写了 42 屏（45 次写入 lint 全过，规则一条没触发），却**改不了这个项目的设计系统**:MCP 只有只读的 `get_design_contract`,于是画布用的是 Rally 品牌色(钴蓝 + 暖米 + Archivo)、真机用的是 iOS 系统色(#007AFF + 纯白 + SF Pro),两边气质对不上而 agent 无从下手。新增 `quilt.update_design_system`（投影到 `API-EDIT-002`，可选 `applyToScreens` 顺带发一次 `apply_design_system` 回刷）与 `quilt.update_project`（投影到 `API-CORE-027`，改名与 brief——错的 brief 会污染 `generate_screens` 的输出）。**不放宽设计契约**:该 agent 明确反对允许默认色板类或内联 style，理由是「改一次 token、42 屏全跟着变」正是严格契约换来的,两套色源会直接毁掉它。 |
| 2026-09-20 | v0.41 | **画布位置可撤销**（`REQ-CORE-018` 补充）：验收反馈「移动或者对齐错了没有退路」——拖卡片与对齐 / 等距都是一步就把屏挪走的动作，此前只能手工挪回去。新增 `⌘Z`：每次动之前把「这几屏原来在哪」压进一个至多 20 步的位置撤销栈，按一次还原一步（toast 点名撤销的是移动 / 对齐 / 等距），栈空时说明「没有可撤销的移动」。**只管位置**——屏内容的历史在修订树里（`⌥R` 回溯），删屏有确认框，都不进这个栈；写回复用同一条位置 PATCH，部分失败就重取并说明。 |
| 2026-09-20 | v0.40 | **项目重命名与设计预设**：① `REQ-CORE-002` 补**重命名**——项目切换器每行 hover / 键盘高亮时,垃圾桶旁边多一个重命名键,点它该行的名字就地变成输入框,回车或失焦保存(`API-CORE-013`,名字已被占用不拦——项目名不唯一),`Esc` 放弃;② 新增 `REQ-CORE-021` **设计预设**——把当前项目的设计系统连同素材存成账号级预设(`ENT-DesignPreset`),新建项目时可选一个预设开局,已有项目也能在设计系统面板里随时套用(套完问一次是否回刷所有屏)。预设存的是**输入**(种子色 / 字体 / 圆角 / 品牌色板 / 模式 / DESIGN.md / 组件配方)而不是算好的 tokens,套用时按当时的 token 引擎重算——引擎改了(如 v0.35 补语义色)老预设跟着受益;素材是二进制,按值复制一份到目标项目,两边此后各改各的。新增 `API-CORE-033`,迁移 `0011`。 |
| 2026-09-19 | v0.39 | **v0.38 收尾**（审查报出的残留，三条是 v0.38 自己引入的）：① 保存设计系统后只有**改到 token 的那几项**（种子 / 字体 / 圆角 / 色板 / 模式）才问「回刷所有屏？」——只改 DESIGN.md 正文时回刷产出的是逐字节相同的新修订加 N 次截图；② 该弹层关掉后焦点原本还给「保存」，而保存成功后它已变成原生 `disabled`、`focus()` 静默失败导致焦点掉到 `body`（之后按 `Esc` 会被画布接走去取消作业），改为兜底落到「回刷所有屏」；③ 折叠横条在「恰好一个作业且还没有进度事件」时退回「对话 · N 条」，改为与在跑作业行同源的「<动词> · 排队中…」；④ 素材响应改 `Access-Control-Allow-Origin: *`（见 `ADR-017`）；⑤ 素材类型嗅探收紧——只认文件头部合法前缀之后就是 `<svg` 的，此前任何内嵌一段 SVG 的 html / txt 都会以 `image/svg+xml` 入库并在屏里裂图；⑥ 素材端点统一 `422`（此前「缺少 file 字段」仍回 400，同一个 problem type 对应两个状态码）；⑦ 色板表在「切了亮/暗但还没保存」时不再谎报上一模式的派生值；⑧ 本机 agent 面板的「完成」药丸用的是不存在的 `text-ok`（未定义的 token 类被静默丢弃），改 `text-success`。 |
| 2026-09-19 | v0.38 | **验收缺陷回写（八组，一半是实现追齐设计、一半是设计追齐实现）**：① **画布**——排列条与候选就地展开互斥（候选胶囊挂在每格上方、与排列条争同一条横带 y 64~106，排列条压在上面，点「收起」会点成「右对齐」并把新位置落库）；排列条改为在可用区内居中并避让右侧浮层（`--chrome-right`），48rem 以下面板叠在画布上时整条让位；`role=toolbar` 补工具条键盘模式（roving tabindex + 方向键 / `Home` / `End` 环绕）；「造完自动选中新屏」收窄为**目标为空时**才接管（后台一批屏造完不再把用户手写的目标与草稿改投到新屏）；刚写出去的坐标在服务端回声之前归本地（在途刷新的旧快照整份写回会把排好的位置顶回去，且此后没有任何路径再纠正）；位置 PATCH 改为等全部 settle 再决定，失败 toast 点名哪几屏没排上、成功的不回滚；`Esc` 的「先关弹窗」明确含 `?settings=` 设置弹层（此前焦点掉到 body 时按 `Esc` 会取消在跑作业）；`generate` 带 `fromScreenId` 时占住入口屏（`REQ-CORE-020` 覆盖屏集）。② **卡片层**——「局部修改中」药丸与候选角标同挂卡片右上时错开一档；候选角标从卡片里提出来成为兄弟层（卡片带内联 `transform`，自成层叠上下文，角标留在卡片里时 z-index 再高也被展开层盖住，展开态点不到「收起」）；候选 iframe 的 `Esc` 放行为例外（收起展开层）；`.viewport` 的 `overflow: hidden` 改 `clip`——留成滚动容器时浏览器的 scroll-into-view 会把整块画布推走（实测 `scrollLeft` 跑到 699、卡片屏幕 x 由 896 变 197），屏幕↔世界换算随之整体偏 `scrollLeft/zoom`。③ **候选整组采用**按返回值分三支报数（全采用 / 部分跳过 / 全跳过），零采用既不刷新也不收起——服务端逐屏 try/catch 后整体恒 `200 {adopted, skipped}`，此前一律报成功并自动收起。④ **设计系统与素材**——保存成功后问一次「回刷所有屏？」（`ds-apply-ask`），有未保存改动时回刷按钮不可点（回刷作业只带 `screenIds`，worker 用的是库里那份）；素材区补拖拽上传与「有 N 个文件不是 SVG / PNG / JPEG / WebP，已跳过」的就地说明；展示用 `<img>` 去掉 `crossOrigin`（`/a/` 的 ACAO 是常量 `webOrigin`，非标准 origin 下整张图不显示，而亮度探测失败本就回落棋盘格）；暗色下删掉最后一个品牌键改发 `colorMode:'light'`。⑤ **token 派生按 `colorMode` 取 Material 对应方案**（此前暗色模式拿亮色方案：`text-on-surface-variant` 压在 `bg-surface-variant` 上只有 1.39:1，改后 5.19:1），语义色明度同档切换（亮 40/100、暗 80/20，与 `error`/`onError` 同规则）。⑥ **`API-EDIT-002`**：400 只在**请求显式**选 `dark` 且无暗色色板时报；暗色色板被这次请求撤掉时 `colorMode` 自动回落 `light`（否则存出「dark 且无暗色色板」，而面板的亮 / 暗段控挂在 `palette.dark` 上，段控消失、模式却停在暗）；`dark:{}` 归一为「没有暗色色板」。⑦ **素材接口**：`mediaType` 由文件正文魔数认定（浏览器按扩展名给的 `Content-Type` 不可信——改名的 webp 会以 `image/png` 下发，屏里永远渲染不出来），像素尺寸经有限性与 `MAX_DIM` 夹取（此前 NaN 与天文数字直接插 `int4`，webp 改名报 500、坏 `viewBox` 报 500），50 个上限的「数 + 插」进同一事务并先锁项目行（60 路并发直传实测全部 201），四处校验错误码统一 422；迁移 `0010` 的四个语义色键改由**启动时回填**补齐存量 `tokens`。⑧ **本机 agent 面板**改为轮询 `API-CORE-029`（1.5 s，无在跑作业即停、后台标签页跳过、失败即停并给「重试」），不再按作业各开一条 `API-CORE-008`——5 个 agent 作业同时跑就占满同源 6 条额度；「已投递」直接读 `output.delivery`（落库的事实），不再另存一份状态。⑨ **在跑作业行的进度兜底**：项目事件流不落表不重放，认领来的作业（首次加载 / 刷新页面 / MCP 或另一标签页建的）在下一个事件到达前没有进度文案，行上只剩「造 1 屏」、与卡死不可分辨——空进度显示「排队中…」。⑩ 文档口径修正：§13 单屏采用的接口从已删除的 `API-CORE-024` 改回 `API-CORE-025`（`POST /screens/{screenId}/revisions/{revisionId}/adopt`），§14 错误目录标明素材校验为 422。`TC-CORE-029` / `030` / `034` / `035` / `036`、`TC-EDIT-005` / `010` 同步，`tests/e2e/core.ts` 补三条回归断言 | @bug |
| 2026-09-19 | v0.37 | **一个标签页只开一条长连接**（`API-CORE-030` 扩写，`REQ-CORE-005` 补充）：验收问「能不能多开标签页同时做两三个项目」，实测**不能**——浏览器对同源 HTTP/1.1 只给 6 条并发连接，且**跨标签页共用**；画布此前每个在跑作业各开一条作业事件流，3 个标签页各跑 1 个作业就占满 6 条，之后所有普通请求排队 5 s 不返回（实测：3 条空闲 13 ms、5 条 11 ms、第 6 条即超时）。v0.36 的并行作业让一个标签页就能跑多个作业，这个上限更容易撞。改法：画布只订阅**项目事件流**一条，作业进度改由 `job_changed` 携带——`emitJobEvent` 把事件投影到项目频道时带上裁剪过的 `data`（`pg_notify` 上限 8000 字节，`succeeded` 原本带全量屏 DTO，一定要裁），画布据此更新进度行与终态；`API-CORE-008` 作业事件流保留不变（本机 agent 面板与外部消费者仍可用）。 |
| 2026-09-19 | v0.36 | **并行作业**（新增 `REQ-CORE-020`）：验收时一批屏正在造，想同时改另一张已有的屏——输入框整个被锁死（textarea 禁用、发送直接 return、通道与档位一并灰掉），而后端从来允许这么做：串行只来自两把部分唯一索引（同项目两个 `generate`、同屏两个以 `target_screen_id` 落库的作业），worker 有 20 个并发槽。改动：① 画布从「单个在跑作业」改为跟踪一组，进度按 `jobId` 分别记，每个在跑作业各订阅一条事件流；② 输入框永远可输入（textarea、通道 / 会话下拉、屏数 / 版数、参考图入口都不再禁用），只有这一轮与在跑作业真冲突时才挡住发送（发送键 `aria-disabled` + 就地写出理由 `send-blocked-reason`，草稿与参考图留着，撞上后端 409 也不清）；③ 输入框上方多一叠**在跑作业行**（`running-jobs` / `running-job` / `cancel-job`，超 3 行折「+N」），每行写清这个作业在做什么与进度、各带取消键，原先绑单作业的那条进度条由它取代；④ `Esc` 的取消档改为取消**最新**那一个，按一次取消一个；⑤ 三个造屏入口（双击空白放锚点、工具栏「新建屏幕」、断链补屏）的拦截判据从「有任何作业在跑」收窄为「已有 `generate` 在跑」，否则与「造屏 + 改另一屏放行」自相矛盾；⑥ §16 补真值表（谁能与谁并行）并纠正「回刷作业按屏逐个占用」一行——`apply_design_system` 与多屏 `edit_screens` 的 `target_screen_id` 都落 `null`，后端看不见它们实际改的屏，同屏撞车的后果是一方作业 `failed`（`revision conflict`）或静默顶掉对方，因此前端按**覆盖屏集**（含 `runner=agent` 的作业）判定，不按 `target_screen_id`；⑦ §14 `/errors/screen-busy` 的处理列改为「预判 + 点名是哪一屏忙 + 不吞输入」；⑧ 新增 `TC-CORE-036`，`TC-CORE-013` 第 2 步与 `TC-CORE-023` 第 10 步改写（此前把「输入框禁用」当验收点） | @bug |
| 2026-09-19 | v0.35 | **品牌色板与项目素材库**（把 Ofcourt Rally 品牌包接进画布项目时暴露的两个缺口）：① 新增 `REQ-EDIT-005` **品牌色板**——设计系统除种子派生色外可挂一份显式色板（亮 / 暗各一套、逐键覆盖），`colorMode` 决定当前用哪套，外部品牌规范的精确色值（如 Court Chalk `#F4F1EA` 底色）从此进得来；token 色键补 `success / onSuccess / warning / onWarning`，没有覆盖时也由种子按固定色相派生——此前只有 `error`，「把进度条改成绿色」这类要求没有合法 token 可用，模型只能退回主色（`ADR-005` 修订；`API-EDIT-002` 请求加 `palette` / `colorMode`，改完仍走 `apply_design_system` 回刷）。② 新增 `REQ-CORE-019` **项目素材库**——项目级上传 logo / 插图 / 模板，每个素材有稳定 URL 可被屏里的 `<img>` 直接引用，素材清单进每次生成的 prompt；新增 `ENT-Asset`、`API-CORE-032`、预览域 `GET /a/{projectId}/{assetId}`（`ADR-017`）。③ 迁移 `0010`：`design_systems` 加 `palette` / `color_mode`，新建 `assets` 表。④ 验收反馈：素材导进来了却在画布上看不见——风格指南卡片补一行**品牌素材**（直接画出导入的标志），`API-CORE-004` 响应随之带上 `assets[]`，素材区与卡片同一份数据；素材瓦片按该图的平均亮度自动选深 / 浅底，白色版标志不再是一块空白。⑤ 画布空态那句「在下方输入框描述你的 APP，屏幕会生成到这里」删掉——风格指南卡片与输入框占位文案已经把话说完了。 |
| 2026-09-19 | v0.34 | **本机 agent 改为投递到活跃会话**（`REQ-AGENT-003` 改写，`ADR-015` 修订，`REQ-CORE-011` 改写）：验收时问「怎么选具体的本地会话，总不能随便投一个窗口」——v0.32 的实现是每个作业新拉一个无头 `claude -p`，跟任何窗口无关，选不了也没得选。现在「交给本机 Claude Code」= 把作业**投递到本机一个正在运行的 Claude Code 交互式会话**：会话来自 Claude Code 自己的登记处 `~/.claude/sessions/<pid>.json`（只取 `kind=interactive`、`peerProtocol=1`、inbox socket 探活通过的；用户起过名的显示名字，派生名的显示会话 UUID；按最近活跃排序，列全机不按目录过滤，`API-AGENT-010`），投递 = 往该会话的 unix socket 写一行 `{"type":"user","message":{"role":"user","content":…}}`，与 Claude Code 会话间消息同一通道（半公开内部协议，风险与回落见 `ADR-015`）。输入框工具条：选了「交给本机 Claude Code」后旁边多一个**会话下拉**（打开即刷新），选择跨会话记忆；记住的会话已关闭时不自动换人，下拉回到「选择会话」并挡住发送。作业收口改由会话经新 MCP 工具 `quilt.finish_job` 完成（30 分钟没收口 → `failed errorClass=timeout`）；取消 = 作业标 cancelled、该 jobId 的后续回写被拒；进程重启时 `running` 的 agent 作业不再标失败，只重新计时。删除无头拉起路径（`claude -p` / `codex exec`、`QUILT_AGENT_COMMAND`、`$QUILT_HOME/agent`），**Codex 通道随之推迟**（它只有无头一种执行方式；`codex queue --thread` 是文档化的投递命令，列为后续路径）。`TC-AGENT-009` 重写为假会话（登记文件 + socket）验整条链，`TC-AGENT-010` 改为人工，`TC-CORE-025` 第 7 步改写。同轮验收再补两项：③ 预览运行时把 `⌘E` / `⌘/` 也转发给父页（此前焦点在 iframe 里时只转发 Esc / Alt+←，角标承诺的「选元素按 ⌘E」按不动）；④ 检查器「用 AI 重生成这块」有自己的通道 + 会话选择器（`REQ-EDIT-002` 改写，`regenerate_subtree` 输入增 `runner?`），记忆独立于输入框、首次沿用输入框当前通道，选本机 agent 时投递到会话且提示词限定只重写该子树，说明框里 `Shift+Enter` 即重生成；⑤ 预览域下发时把修订里存的运行时换成当前版本（`withCurrentRuntime`，`ADR-003`），运行时修复不必等每张屏再出一版修订；⑥ 发出直改 / 子树重生成后检查器不再退回空态：选中不清、只清说明框，屏换修订后运行时按 qid 重选（`quilt:reselect` / `quilt:deselect`），字段刷成新值；⑦ **项目级事件流** `API-CORE-030`（`GET /projects/{id}/events`）——本机会话经 MCP 回写、别处建的作业、截图就绪都推给画布，画布收到就刷新（聚焦中的屏走热更新），此前这些变化要进出一次交互态甚至刷新页面才看得到；⑧ **屏内修改标记**：子树重生成作业进行中，聚焦屏内该元素虚线描边 + 「修改中…」角标、未聚焦卡片标「局部修改中」，回写后换成绿描边 + 「已更新」4 s 自撤（`quilt:mark`）；检查器对正在改的元素挡住重复发起。为此 `replaceSubtree` 改为**替换后的根沿用被替换元素的 qid**（`ADR-007` 修订），标记、重选、批注才按 qid 对得上；⑨ 签名 URL（截图 / 修订 HTML / 预览）的过期时间对齐到 30 分钟窗口——此前每次 `refresh()` 都换一串新签名，卡片 `<img>` 整批重新请求、全屏闪一下，事件流让刷新变频繁后尤其明显；对齐后同一窗口内 URL 字符串不变，React 不动 DOM；⑩ 删除 v0.27 的「预置 / 本机通道按账号移除、可恢复」（`API-CORE-024`、`users.hidden_runners`、设置页「已从清单移除」折叠区）——本地单用户版没有「其他账号」，要增删预置通道改 `QUILT_RUNNERS` 即可；迁移 `0009`；⑪ 对话记录面板从「贴左侧、顶到底」改为**贴左下角、底部对齐**，头部一整条可点上拉 / 下收，折叠后只剩横条；窄视口下横条停在输入框上方，为此 `--chrome-bottom` 改为跟着输入框实际高度走（`TC-CORE-023` 第 9、11 步同步）；⑫ 选择元素态的选中框常驻（此前唯一的框跟着 hover 走，点完就没了），带「tag · qid」标签；直改保存后该元素也打「已更新」，作业收口时立刻打、换完 DOM 再补（此前只在换 DOM 后打，作业先出屏后收口的路径打不上）；⑬ 删项目（`API-CORE-031`）：项目切换器每行 hover 露出垃圾桶（高亮行 `Delete` 键等价）→ 确认 → 级联删除并清对象文件，有进行中作业 409；⑭ 浏览器标签页标题带项目名；⑰ **多选排列**（新增 `REQ-CORE-018`）：选中 ≥ 2 屏出现排列条，六种对齐 + 两种等距，位置走既有 PATCH 落库；⑯ 候选覆盖层改为**画布就地展开**（`REQ-CORE-015` 改写）：收起态卡片后叠错位底板，点角标在原位把各版横向铺成一行（与卡片同尺寸、随画布缩放，每格是可交互的活 iframe），采用动作收进每格上方右缘对齐的浮层图标胶囊，采用后自动收起；⑮ **删除「系统预置」通道层**（`QUILT_RUNNERS`、`source=preset`、`model:*` 探测）——开源自用，云端通道全部由用户在设置页自己配（`REQ-CORE-011` / `API-CORE-023` 改写，`ADR-013` 结论修订）；没带通道的作业建作业时填默认通道；`pnpm seed` 按 `.env` 的 `GEMINI_API_KEY` 建一条已验证 Gemini 供真实生成用例用；本机现有的三条预置（本机 Claude 订阅、Gemini 3.7 / 3.8 Flash）已迁成用户通道 | @bug |
| 2026-09-18 | v0.33 | 验收反馈两项（`PAGE-CANVAS` 前端行为 + 预览运行时一处协议扩展，无接口 / 数据模型变化）：① **输入框可收起**——`⌘/` 随时切换底部输入框显隐（焦点在输入框内也生效），工具栏「输入框」工具是同一开关；收起后底部什么都不留，只是不显示，草稿、参考图、目标标签都留着，不跨会话记忆；**双击进屏 / 选择元素 / 批注聚焦某屏时自动收起，退出聚焦回到进入前的状态**，聚焦期间可用 `⌘/` 临时叫出；放锚点（双击空白 / `⌥G`）总会把它叫回来；收起时画布安全区底部占位由 11.5rem 缩到 1rem。② **聚焦态热更新**（`REQ-CORE-005`）——此前聚焦期间 iframe 的 `src` 钉住（v0.16），作业完成后只有卡片截图变、屏内仍是旧修订，要退出交互才看得到；现在 iframe 里正显示的那一屏（导航栈顶，没跳转过就是卡片自己）一出新修订——直改、改屏、重生成子树、回刷、采用候选——父页取新 HTML 经 `quilt:swap` 换进同一文档，不重挂、不丢导航栈与模式；`quilt:swap` 增 `keepScroll`，热更新保留滚动位置、跳转仍回顶。元素直改路径随之改走同一通道（不再换 key 重挂）；屏换修订时清空当前选中元素。新增 `TC-CORE-032`，`TC-CORE-023` / `TC-EDIT-001` / `TC-EDIT-003` 增步骤 | @bug |
| 2026-09-18 | v0.32 | **本地单用户版（M10，`ADR-016`）**：产品先做本地自用，SaaS 推迟。① **去账号**——数据模型保留 `users`（一行默认用户 `local@quilt.local`，`owner_id` / `created_by` 照旧指向它，将来加回登录即回到多账号），删除 magic link / 会话 / 登录页 / 邮件驱动（`REQ-CORE-001` 推迟；`magic_links`、`sessions` 表删除）；鉴权中间件把每个请求都解析成默认用户；MCP 免鉴权（`REQ-AGENT-001` 的 OAuth 2.1 推迟，`oauth_*` 表删除）；服务只绑 `127.0.0.1`。② **去硬上限**——`users.quota_*` 删除、`assertQuota` 与 `429 /errors/quota-exceeded` 删除，用量台账保留并在设置弹层可见（`REQ-CORE-008` 改写）。③ **本机 agent 改为 Quilt 进程直接拉起**——「交给本机 Claude Code / Codex」的作业由 `agentRunner` 用本机 CLI（`claude -p` / `codex exec`）执行，MCP 地址就是本机；伴侣进程、设备 token、deeplink、派活队列与租约整组删除（`REQ-AGENT-003` 改写、`REQ-AGENT-004/005` 推迟、`agent_tasks` / `device_tokens` 表删除、`ADR-009` 作废、`ADR-015` 修订）；CLI 不在 PATH 时通道标不可用并就地给安装步骤。④ **一键安装**（新增 `REQ-CORE-017`）——`npx quilt-canvas`：内置 PGlite 代替 Docker Postgres（`DATABASE_URL` 留空即用，数据在 `$QUILT_HOME/db`）、进程内队列代替 pg-boss、前端打成静态文件由 API 进程托管、截图优先用本机已装的 Chrome / Edge、首次启动自动生成 `~/.quilt/config.env`（密钥）并打开浏览器（`ADR-010` 修订）。新增 `API-CORE-028`（运行时配置）、`API-CORE-029`（作业列表）；§11 `AgentTask` 状态机删除；§12 时序 3/4 改写；§26 增里程碑 M10 | @bug |
| 2026-09-18 | v0.31 | **动词版功能重整（M9）**：三条原则——① 动词由目标决定：输入框有目标标签 = 改，没有 = 造；② 版本只有一种：修订，候选 = 同一作业产出的兄弟修订；③ 设计系统是唯一的持久记忆，只显式改。改动：**造**——`generate_screens` / `generate_screen` / `generate_missing_screen` 三种作业合并为 `generate`（`REQ-CORE-003`、`REQ-CORE-014`、`REQ-PROTO-003` 改写）：屏数档位 `1/2/3/4/自动`（非空项目默认 1、空项目默认自动）、版数 1–4、双击空白 = 放锚点并聚焦输入框（新屏浮框删除）、一组屏按流程顺序摆成一行、规划器声明入口屏时对其跑一次「接上跳转」（反向连线）、造完自动选中新屏；**改**——目标标签只被「点屏 / 标签 × / 清空」改变，点空白只清画布高亮不清标签（`REQ-CORE-006`），动词行写全「改 3 屏 × 2 版 = 6 次调用」，`⌘A` + 发送 = 普通改全部；**选**——修订链改为分支（`ENT-ScreenRevision` 增 `parentRevisionId` / `candidateIndex` / `candidateSettledAt`，`REQ-CORE-007` 改写，非目标 ⑤ 撤销），候选覆盖层列 = 版本、行 = 屏，整组或单格采用（`API-CORE-025` / `API-CORE-026`）；**设计系统**——新增应用简介与样板屏（`ENT-Project` 增 `brief` / `exemplarScreenId`，`API-CORE-027`）、DESIGN.md 新增「## 约定」节，改法只有面板指令输入与回执「记为约定」两条显式路径，经作业 `propose_design_system` 提炼成绝对规则、预览确认后写入（`REQ-EDIT-003` 改写）；**本机 agent** 定为同一套动词的另一位执行者（`ENT-GenerationJob` 增 `runner`，`ENT-AgentTask` 增 `jobId`，任务状态映射到作业状态，租约过期释放屏锁并回队、重领时重新快照，带 `jobId` 的 `update_screen` 必须传 `expectedRevisionId`，`REQ-AGENT-003` 改写，`REQ-AGENT-005` 推迟）；**用量**——删除 12 次调用封顶，余额 = 本月实际 + 在途预估（从 queued / running 作业派生、不落库），发送前 / 建作业 / 重生成确认共用一个预估函数，作业超时随预估伸缩（`REQ-CORE-008`）；**上下文**——`ADR-012` 改写为「稳定前缀（规则 → DESIGN.md 含约定 → 配方 → 应用简介 → 屏注册表 route/name/purpose）+ 情境层（造：样板屏 + 来源屏；改：目标屏当前 HTML + current 祖先链上的指令 + 样板屏）」，`ENT-Screen` 增 `purpose`。新增 `ADR-014`（候选即修订）、`ADR-015`（agent 为执行者）。§19 增 6 个场景，§26 增里程碑 M9。**实现偏差回写**：① 「改」的作业 kind 名保留 `edit_screens`（MCP 工具名与既有契约不动，文档里的 `revise` 即它）；② 候选覆盖层每格的「采用这一版」放在缩略图**上方**——手机屏一版比覆盖层还高，按钮在图下就得先滚到底才够得着，且行首留 `scroll-margin-top` 不钻到 sticky 列头底下；③ 输入框因目标区常驻而增高到约 162 px，画布安全区底部占位 `--chrome-bottom` 由 9.5rem 调到 11.5rem，聚焦屏底被压住的那一段按 v0.19 的规则留给可滚动区（`TC-PROTO-001` 改为先在画布空白处滚一下再点屏底按钮）；④ 屏数档位的默认值在前端按「项目此刻是否为空」取（空 → 自动、非空 → 1），只在空 ↔ 非空切换时重置；⑤ 新屏落地即派生应用地图，不等作业里随后的反向连线跑完（懒生成补屏后断链立刻解析）；⑥ 弹层（候选覆盖层 / 提案预览）里点过一个随即消失的按钮后焦点会掉到 body，`Esc` 由画布快捷键兜底关闭，覆盖层采用后把焦点收回容器 | @bug |
| 2026-09-18 | v0.30 | 验收反馈三项（均为 `PAGE-CANVAS` 外观，无接口 / 数据模型变化）：① **通道下拉只给厂商图标 + 显示名**——模型 id（`claude-sonnet-5`、`gemini-3.8-flash`…）从每项下方的副标题里去掉；选通道时认的是名字，模型 id 是配置细节，它的家在设置弹层的通道管理器（那里每行仍写明类型 · 模型 · 端点）。② **画布顶栏不再挂品牌**——去掉左上的「Quilt /」，第一个元素直接是项目切换器；字标改为只出现在非画布 chrome（登录页、`PAGE-FIRST`、授权页），§13.1 的「字标用于所有 chrome」随之收窄。③ 新屏浮框的屏数分段控件内外圆角同心（内层半径 = 外框 `--radius-lg` 14px − 内边距 2px），此前内层用 10px，选中块的弧线与外框对不上 | @bug |
| 2026-09-17 | v0.29 | 验收反馈三项：① **画布指针**——默认箭头；按住空格（平移就绪）为抓手、平移中为握拳；卡片只在拖动中为握拳。此前画布常态就是抓手，与「空白处按下是框选」的语义打架。② **设置弹层改分栏**——左栏是五节导航（本月用量 / 已授权的 agent / 伴侣进程 / 生成通道 / 账号，Radix Tabs 垂直方向，上下方向键换节），右栏只显示当前节；当前节进 URL `?settings=usage|grants|devices|runners|account`（旧 `/settings` 落到 `usage`，不认识的值也回 `usage`），输入框「查看用量」打开 `usage` 节、「管理通道…」打开 `runners` 节。③ **多屏生成改为独立屏，候选抽卡作废**——`generate_screen` 的数量 > 1 时直接生成 N 张互相独立的正式屏（同名，路由 `/x`、`/x-2`…；第一张落在双击点、其余向右排开），用户随时删掉不要的；不再有 `status=candidate` 的修订，`ENT-ScreenRevision.status`、`ScreenDto.candidates`、`API-CORE-025` pickCandidate 一并撤销（v0.28 ③ 作废，`API-CORE-025` 编号空缺）。新屏**不强制与既有屏连线**：规划时 `links` 可为空且以空为常态，出屏提示随之写明「这一屏暂不需要跳转到别的屏」（去向未定的动作用 `href="#"`），交互想清楚后再用「接上跳转」或懒生成补上——先把屏设计出来，交互是后面的事 | @bug |
| 2026-09-17 | v0.28 | 三项（M8）：① **导航模型改为「画布即主界面」**——取消独立的项目列表页与设置页。`/` 直接进最近更新的项目；顶栏左上的项目名成为**项目切换器**（点开为按更新时间倒序的列表，当前项打勾，底部「新建项目…」打开原新建弹窗）；顶栏「设置」打开**设置弹层**（原设置页五节原样搬入，`?settings=1` 进 URL 刷新保持；旧 `/settings` 与 `/projects` 路径重定向），「返回项目」按钮随之删除；弹层基建抽成 `components/modal.tsx`，`inert` 改为**层栈计数**以支持设置弹层里再开「添加通道」（开一层隔离下面所有层，关一层只放开新的最上层，栈空才解除 `#root` 的隔离），新建项目弹窗一并接入。② 新增 `REQ-CORE-014` **在画布空白处凭空生成一屏**——双击空白（单击是清空选择、拖拽是框选，都不能占用）或工具栏「新建屏幕」`⌥G`，在点击位置弹出新屏浮框：描述、通道、生成数量 1–4；新作业类型 `generate_screen`：先一次小调用把描述规划成 `{name, route, purpose, links, sections}`（路由不得与现有撞车、链接只指向现有路由），再在点击位置插屏行并并行出屏。用户截图里的痛点正是缺这个入口：「生成一个额外的屏幕」被输入框按「未选中 → 改全部屏」处理成了改五屏。③ **候选抽卡**：数量 > 1 时每个候选以 `status=candidate` 落为该屏的修订、**不**推进 `current_revision_id`；卡片在原位以 2 列网格铺出候选缩略图，各带「选这个」，`API-CORE-025` 选定后该修订成 main 并成为 current、其余 discarded；未选定前该屏不进应用地图与导出（导出本就只看 current；应用地图的节点自本版起也只列有 current 的屏）。每个候选按 1 屏计费。`ENT-ScreenRevision` 增 `status`，修订面板只列 main | @bug |
| 2026-09-17 | v0.27 | `REQ-CORE-013` 验收反馈：**系统预置与本机通道也可从清单移除**。它们来自服务端 `.env` / 固定内建，所有账号共用，因此「删除」实现为**按账号隐藏、可恢复**——不动服务端配置，但从该账号的设置页主列表与画布输入框下拉里消失；设置页底部保留「已移除 N 条 · 恢复」入口，逐条或全部恢复。持久化在 `users.hidden_runners`（runner id 数组），新增 `API-CORE-024`（`POST /runners/{id}/hide` / `unhide`），`RunnerOptionDto` 增 `hidden`；默认通道的选择跳过已隐藏项。账号自建通道仍是真删除，不走这条 | @bug |
| 2026-09-17 | v0.26 | `REQ-CORE-013` 验收反馈四项：① **本机 Claude 订阅也能自己建通道并改模型名**——新增通道类型 `agent-sdk`，它用服务端机器上的 `claude` 登录态，不填 Key 也不填端点，只选模型；因此 `createChannelSchema` 的 `apiKey` 改为可选（仅该类型可缺），`QUILT_SECRETS_KEY` 也只在真要存密钥时才要求；② **探测超时按驱动分档**——`agent-sdk` 每次要拉起子进程并带约 14K token 缓存前缀，实测冷启动 2.5~3.5 s、抖动时更久，与 HTTP 驱动共用 15 s 会把「慢但可用」误判成不可用；改为 agent-sdk 60 s、HTTP 驱动 20 s、stub 5 s；③ **可用性与探测结果在界面上分离**——此前预置通道会同时显示「可用」药丸与「验证失败」红字，自相矛盾；现在药丸只表达可用性（预置 = 凭据是否就位、自建 = 验证结论），预置项的探测失败措辞改为「这次没验证通过」；④ **表单下拉统一用 Radix Select**（与 shadcn 同底座），抽出 `components/Select.tsx`，仓内五处原生 `<select>` 全部换掉（通道类型 / 厂商预设 / 字体 / 跳转目标 / 任务类型）——原生列表由系统渲染，暗色主题下在部分平台会画成亮色面板，也接不了图标与分组说明。另修：模型名输入框被浏览器密码管理器自动填成邮箱，`autoComplete="off"` 在 Chromium 上常被忽略，改用语义不相干的具体值加两家密码管理器的忽略属性 | @bug |
| 2026-09-17 | v0.25 | 新增 `REQ-CORE-013` **生成通道可配置**（同日实现，`RUN-053`）。产品定位：先做开源自部署、本地自用，不堵死将来的 SaaS。① 设置页的「生成通道」从只读清单改为**管理器**：API 类通道（Anthropic / Gemini / OpenAI 兼容通用端点）由用户在弹出面板里填显示名、Endpoint、API Key、模型名；本机三条通道（本机 Claude 订阅、本机 Claude Code、本机 Codex）不可创建，以固定行呈现并就地给出**如何配置**的步骤；每一行都有**验证**按钮，实际发一次最小请求探测可用性；② **只有验证通过的通道才进画布输入框的下拉**，设置页照样列全并写明原因（`/v1/runners` 仍返回全部项，前端按 `available` 过滤；此前「不可用项灰化列出」的做法移到设置页）；③ 密钥落库、以 `.env` 的 `QUILT_SECRETS_KEY` 派生的 AES-256-GCM 密钥加密（`ADR-013`），接口只回显掩码后 4 位，日志全链路不打印；④ 配置按**账号**隔离，`.env` 里的 `QUILT_RUNNERS` 预置项作为「系统预置」对所有人可见但不可改；⑤ 新增 OpenAI 兼容驱动（`Endpoint + Key + 模型名`，DeepSeek / Qwen / Moonshot / 智谱 / OpenRouter / Ollama / SiliconFlow 等以厂商预设填默认端点），LLM 驱动工厂由「按驱动名取单例（凭据来自 env）」改为「按（驱动、端点、凭据）取实例」；⑥ 通道以厂商彩色图标标识（`@lobehub/icons`）。新增 `ENT-Channel`、`API-CORE-020`～`023`、`ADR-013`、里程碑 M7。**实现偏差回写**：① 「本机 Claude 订阅」（`agent-sdk`）来自 `.env` 的 `QUILT_RUNNERS`，在设置页归入「系统预置」组并带配置步骤，「本机通道」组只剩两条派活项；② 输入框下拉末尾的「管理通道…」做成 listbox 里的哨兵 `option`（选中只跳设置页、不改通道）而非普通按钮——Radix Select 的内容区只让方向键在 option 间移动，按钮键盘够不着；③ 添加 / 编辑面板与删除确认用 `inert` 隔离背景实现焦点陷阱并抽成 `useModal`，项目里更早的三个弹层（删屏确认、缺屏提示、新建项目）尚未接入，见 §28 | @bug |
| 2026-09-17 | v0.24 | `REQ-CORE-012` 补齐**本机三条通道的图片传递**（v0.23 初版误判 `agent-sdk` 不支持视觉，本版更正）：① **agent-sdk 驱动**——`query()` 除字符串外接受 `AsyncIterable<SDKUserMessage>`，其 `message` 就是 Anthropic 的 `MessageParam`，内容块可含 image；带图时切到这个形态，`vision=true`；② **MCP 通道**——派活 payload 由 `referenceImageUrls` 改为 `referenceImages`（key + mediaType + 签名 URL），`quilt.await_task` 领到任务时把图作为 image 内容块与任务 JSON 一起返回（MCP 工具结果本就允许 text + image 混排，agent 同一刻就看得见），另开资源 `quilt://projects/{projectId}/attachments/{attachmentId}` 供重取；③ **伴侣 socket 通道**——伴侣进程领到任务后立刻把图下载到临时目录（签名 URL 只有几分钟），以本地文件路径写进给 Claude Code / Codex 的提示词（两者都能直接读图片文件），任务结束整目录删除。三条路的图片正文都不经过队列 payload。④ 实证时撞到一次伴侣 ack 被回 `task.gone` 而任务仍 queued——`claimById` 的 `FOR UPDATE SKIP LOCKED` 与周期性租约/过期扫描撞锁时会把可领的行跳过；ack 处理改为「空手且任务仍 queued 则 200 ms 后重领一次」，伴侣不再因瞬时锁竞争放弃任务（此前只能靠下次重连补发） | @bug |
| 2026-09-17 | v0.23 | 新增 `REQ-CORE-012` **参考图输入**：底部输入框支持贴 / 拖 / 选图片，随这条消息作为**参考**进模型。① 语义是「照着这个感觉做」，不是把图复刻成屏——复刻属于 ingest 路径，本次不做；② 作用于消息驱动的两类作业（`generate_screens`、`edit_screens`），补链 / 批注 / 子树这些非自由指令不带图；③ 每条消息至多 4 张、单张 ≤ 5 MB，限 PNG / JPEG / WebP；④ 新增 `API-CORE-019` 附件直传（签名 PUT，与已有 `API-AGENT-009` 同一套签名机制），`API-CORE-010` 请求增 `attachmentIds`，`ENT-Message` 增 `attachments`；⑤ LLM 适配层的 `complete()` 增 `images` 入参，三个驱动都实现视觉输入；每个驱动声明 `vision` 能力并经 `/v1/runners` 透出，不支持的驱动在建作业时就被挡住（护栏保留给将来的驱动，当前无驱动触发）；交给本机 agent 的路径见 v0.24；⑥ 图片消耗的 token 由各家 usage 如实返回，已有用量台账自然覆盖。新增 `TC-CORE-027`。同轮修掉一个会整进程崩溃的缺陷：`emitJobEvent` 用 `.finally()` 清理串行链，而 `.finally()` 派生的是一条新 promise，写事件失败时那条无人接管，一次外键冲突（作业已被删却还在写事件）就以未捕获拒绝把整个 API 带走；改为 `then(cb, cb)` 派生总是 resolve，错误只沿调用方那条链传 | @bug |
| 2026-09-17 | v0.22 | 接入真实品牌包时暴露的两处缺口（`REQ-EDIT-003`）：① 可选字体白名单 `FONT_FAMILIES` 增加 **Archivo**（Google Fonts 有，预览 prelude 按名取用即可），并把前端设计系统面板里那份重复的字体清单收敛到这一个源；② 风格指南卡片的字体名此前写死成 `Inter`，项目换字体后卡上报假信息，改为读项目自己的 `tokens.typography.fontFamily`。同时记录一条已知局限：风格指南卡渲染的是固定样例，不读项目的 `components` 配方，自定义配方后卡片与实际产出会不一致 | @bug |
| 2026-09-17 | v0.21 | 验收反馈（`PAGE-PROJECTS` 前端表单，无接口 / 数据模型变化）：**新建项目只问名称与设备形态，不再让用户一上来定种子色**。建项目时用户脑子里还只有「要做个什么 APP」，品牌色是看到屏幕之后才有判断依据的东西，摆在第一步既提不出好答案、又把必填项从两个变成三个。种子色改为服务端默认值（`#3B5BDB`，与此前表单的预填值相同，故已有项目与新项目的产出一致），生成后随时可在设计系统面板里改并一键回刷全部屏（`REQ-EDIT-003` 已支持）。`API-CORE-003` 的 `seedColor` 仍是可选字段、契约不变——MCP 的 `quilt.create_project` 等调用方仍可显式指定 | @bug |
| 2026-09-16 | v0.20 | 验收反馈（`PAGE-CANVAS` 前端几何，无接口变化）：**底部输入框不再随侧边浮层开合而平移**。此前它在「对话面板与工具栏之间的空白区」居中，于是折叠对话面板会把它横向搬走 134 px（1440 视口实测）——输入框是主输入控件，光标与注意力都在那里，一个侧边面板的开关不该给它搬家。改为**以视口居中为默认，只在真的会被浮层压住时才让位**（`clamp` 取「浮层内边界 / 视口居中 / 另一侧浮层内边界」三者）。阈值可算：视口 ≥ 1368 px 时对话面板的开合完全不移动输入框（实测 1440 视口位移 0 px），窄于此两者真会重叠才平移；右侧面板滑出时同理，只退到与面板留出 1rem 间距为止（1440 视口 32 px） | @bug |
| 2026-09-16 | v0.19 | **品牌接入**（前端外观，无接口 / 数据模型变化）：采用品牌包 `brand/Quilt-Brand-Design`（Q-F-v6，造型 Q-F-v3，主色石墨灰 `#56616E`）。① 新增 §13.1 品牌一节，定下**工具外壳用品牌、用户设计稿不用品牌**这条边界；② 配色改为从品牌语义色派生——外壳深底落在品牌 Ink `#142033`，前景取品牌 QA 已验证的深底组合 `#D8E2EE` / `#B4C1D2`，动作色用界面蓝 `#356FD6`（白字 4.78:1），深底上的强调文字与 hover 用雾蓝 `#769BB9`（5.57:1）；主色石墨灰**不作动作色**（深底轮廓对比偏低，品牌要求配浅色承载面）；错误 / 警告 / 成功状态色不动（`BRAND.md` §4 明确品牌色不替代状态色）；③ 西文与字标改用 Space Grotesk（自托管 woff2 49 KB，权重轴 300–700），中文走系统字体；④ 顶栏、对话记录的品牌字样改为字标排版（Space Grotesk 500 / `-0.03em`），完整符号只用在登录页与 favicon——品牌规定符号下限 64 px、横向组合下限 240 px，顶栏放不下；⑤ 生成中骨架占位改用品牌 Paper→Line 灰阶；⑥ **登录邮件接入品牌**（浅底 Paper / Ink / 界面蓝，全内联样式、不放图片、字体退回系统无衬线——邮件客户端会剥外链样式、拦远程图片；文案随产品改为中文）。同轮修掉两个缺陷：**聚焦屏的纵向落点**改为在安全区（已让开底部输入框）内居中、屏高于安全区时顶到上沿——此前按「顶栏以下的全部高度」居中，v0.18 输入框变高后正好把屏底的主按钮推进输入框底下点不到（`RUN-045` `TC-PROTO-001` 实证）；聚焦角标里残留的 `⌥E` 提示按 v0.17 改为 `⌘E`。新增 `TC-CORE-026` | @bug |
| 2026-09-16 | v0.18 | 验收反馈（`PAGE-CANVAS` 前端外观，无接口变化）：**底部输入框按参考件（Motoko UI AiPromptInput）重做**——① 外壳改为**实底**大圆角面板（2px 边线 + 分层投影，聚焦时加深投影、提亮边线，不再套强调色描边），不再走半透明 chrome；② 输入区加大到 15px / 28px 行高，上限 8 行；占位文案改为叠在输入区上的可动层：空闲且未选中屏时轮播示例、聚焦即隐（原生 `placeholder` 保留给读屏），有目标时显示对应的单条提示；③ 目标标签移到输入区上方，改为实心药丸；④ 工具条以分隔线与输入区分开：左端**通道选择改为自定义列表**（Radix Select：按「云端模型 / 本机 agent」分组、当前项打勾、缺凭据的通道灰化且就地写明原因、方向键 / Enter / Esc 可操作、关闭后焦点回到触发器），中间目标说明（窄屏隐藏），右端**发送 / 停止改为 40px 反色实心圆钮**；⑤ 下拉开着时的按键不再漏给画布快捷键（此前 `Esc` 会顺手清掉画布选中）。前端新增依赖 `radix-ui`（仅 Select） | @bug |
| 2026-09-16 | v0.17 | 验收反馈三项：① **导出原型取消快捷键**（低频且不可撤销，不值得占一个组合键）；② **`⌘E` 改绑「进入 / 退出选择元素模式」**；③ **选择元素与批注改为全局模式**——此前必须先选中某一屏才能开启，现在按键即开，画布光标变准星并提示「点任意一屏开始」，单击哪一屏就进哪一屏；退出某一屏不再关闭模式（`Esc` 只退出该屏），方便连着批注多屏。两个模式因此从「上下文工具组」移到工具栏常驻组 | @bug |
| 2026-09-16 | v0.16 | 实现期缺陷修复（`REQ-CORE-005` / `REQ-EDIT-001` / `REQ-EDIT-004` 共用）：聚焦期间必须**钉住 iframe 的 `src`**——`previewUrl` 是签名 URL，每次刷新项目都会换一串新签名，`src` 一变 iframe 就静默重载并掉回 `interact` 模式，选择元素态与批注态当场失效（元素直改路径本就有此问题，只改一次时看不出来）。随之而来：屏内容变化后需**显式**重载聚焦屏（`onEdited` 走一次「清空钉住值 → 由当轮 props 重新钉住 → 换 key 重挂」），并在每次收到 `quilt:ready` 时重发当前模式，任何重载都不再丢模式 | @bug |
| 2026-09-16 | v0.15 | M6 实现完成（`REQ-EDIT-004`、`REQ-CORE-011`）并按 `docs/TEST.md` 执行（`RUN-037` / `RUN-038`）。实现偏差回写：① 批注态与选择元素态共用屏内 inspect 运行时（都靠点元素），仅面板与去向不同，角标分别显示「批注中」「选择元素中」；② `ProjectDetailDto` 增 `annotations`（项目内未处理批注），画布一次取全、不必逐屏拉；③ `usage_entries` 增 `driver` / `model` 两列，台账按通道分列；④ `/v1/runners` 返回可选通道清单，缺凭据的驱动照样列出但标 `available=false` 并给出原因（凭据仍只在服务端）；⑤ `quilt:select` 的 `rect` 由视口坐标改为**文档坐标**（含滚动偏移），与 `fullPage:false` 截图同一坐标系 | @bug |
| 2026-09-15 | v0.14 | 验收反馈三项：① **「选择元素」可直接进入**——此前必须「双击进交互 → 再点选择元素」，中间那步没有存在理由；现在选中一屏后按工具或 `⌥E` 即进入选择态。**交互态与选择元素态互斥**，退出任一都回到静态截图；从交互态切进选择态时若已在屏内跳转过，先把 iframe 拉回卡片自己的那一屏并清空导航栈，否则选中的元素与卡片截图不是同一屏、批注锚点会错位（§13 工具栏描述更新）。② **批注气泡不再需要实时订阅协议**（ADR-003 改写、`API-EDIT-003` 请求增 `rect`）：卡片未聚焦时本就是静态截图，只需在批注那一刻由 `quilt:select` 多回传一个元素矩形并随批注存下，气泡此后完全由画布层绘制——省掉原方案的 `quilt:track` / `quilt:rects` 订阅、`ResizeObserver` 常驻观察与导出侧改动 | @bug |
| 2026-09-15 | v0.13 | 新增两项功能（M6，尚未实现）：① `REQ-EDIT-004` **元素批注**——聚焦屏内给元素挂自然语言改动说明，画布上以编号气泡常驻，可逐条发或一起发；新增 `ENT-Annotation` 与 `API-EDIT-003`（含 `annotations/send`），**发送按屏合并成一条 `edit_screens` 整屏指令**（N 屏 = N 次计费，而非 N 条批注 = N 次），qid 失效不阻塞发送、靠记录的原文案片段兜底；ADR-003 补预览运行时的 `quilt:track` / `quilt:rects` 两条消息（父页画不出跨域 iframe 内元素的几何，必须由运行时回传，且只在批注态订阅、不进导出）。② `REQ-CORE-011` **生成通道可选**——输入框左下角下拉同时列云端模型与「交给本机 Claude Code / Codex」，`API-CORE-010` 请求增 `runner` 字段；实现时 LLM 驱动须从进程单例改为按作业解析，用量台账按驱动 / 模型分列，凭据仍只在服务端。§19 增 3 个场景，§26 增里程碑 M6 | @bug |
| 2026-09-15 | v0.12 | 验收反馈两项：① **退出账号从顶栏移到设置页**——顶栏右上角改为「返回项目」。退出账号是低频且不可逆的操作，放在每个画布页的右上角既容易误点、也不是「我要去哪」这个位置该承担的语义（`IA-007` 寻路四问里的「怎么退出」指的是退出当前上下文，不是退出账号）。§13 页面地图补 `PAGE-SETTINGS -->|退出登录| PAGE-LOGIN`。② **聚焦态补元素选择的可发现性**——聚焦卡片的角标在可交互后追加提示「改某个元素用「选择元素」」，进入选择模式后改为「点屏里的元素来选中」；此前这个入口只以一个图标出现在工具栏下半段，实测用户双击进入交互后找不到它 | @bug |
| 2026-09-15 | v0.11 | 验收反馈两项（`PAGE-CANVAS` 前端行为，无接口变化）：① **`Esc` 在生成中即取消生成**——优先级为「关弹窗 → 退出聚焦 → 取消当前作业」，取消入口在底部输入框并标出 `Esc`。② **「补链」改为对选中的屏生效**（此前固定取项目前 20 屏、无视选中），未选中时仍作用于全部屏（上限 20）；按钮文案由「补链」改为「接上跳转」，因为「补链」二字没能说清它做什么。关系推断本就由 AI 承担（固定指令给出全部允许路由，让模型自己判断每个导航动作该去哪，无对应屏的写 `href="#"`），本次不改 | @bug |
| 2026-09-15 | v0.10 | 验收反馈三项（均为 `PAGE-CANVAS` 前端行为，无接口 / 数据模型变化）：① **多选屏幕作对话上下文**——`Shift`/`⌘` 加选 + 空白处拖拽框选 + `⌘A` 全选，选中集合作为 `targetScreenIds` 传给 `API-CORE-010`（契约本就允许至多 20 屏，此前前端只传 1 屏），输入框以可移除标签列出目标；空白处拖拽由「平移」改为「框选」，平移保留滚轮 / 触控板 / `空格+拖拽`。② **快捷键分档防误触**——单键只留 `F`/`L` 这类可一眼看出且可撤销的视图操作，开关面板的改为 `⌥` 组合。③ **工具栏悬停提示补作用说明**——图标按钮没有可见文字，提示必须同时给出「这个工具干什么」与快捷键；提示改为渲染在工具栏滚动容器之外（`overflow-y:auto` 会连带把 `overflow-x` 变成 `auto`，贴边提示会被整块裁掉）。§19 增 1 个场景 | @bug |
| 2026-09-15 | v0.9 | `PAGE-CANVAS` 外壳从「顶栏 + 左侧面板栏 + 右侧对话栏」三栏改为**画布铺满 + chrome 浮于其上**（§13 关键页态改写）：顶栏只留身份与账号（项目名、设备、屏数/缩放、设置、退出），画布工具收进右侧竖排工具栏并配快捷键（`F` 适配视图 / `L` 连线 / `D` 设计系统 / `T` 派活 / `R` 修订 / `E` 选择元素 / `⌘E` 导出原型），对话输入移到底部居中，对话记录移到左侧可折叠浮动面板，四个面板（修订 / 设计系统 / 检查器 / 派活）改为从右侧贴工具栏内侧滑出。材质：顶栏为自上而下的渐暗遮罩（无底板、画布透上来），其余三处浮层为半透明 + backdrop-blur，`prefers-reduced-transparency` 一律降级实底。§19 增 1 个场景。纯前端外壳改动：无接口、数据模型、状态机变化，面板状态仍按 `INT-020` 进 URL `?panel=` | @bug |
| 2026-09-14 | v0.8 | LLM 适配层增 `gemini` 驱动（Google AI Studio key，`@google/genai`；§17 依赖表更新）。实测（同一契约提示串行单屏）：gemini-3.8-flash 13–16 s 契约稳定通过，整套 6 屏 118 s、首次 6/6 通过，**选为 gemini 驱动默认模型**；gemini-3.7-flash 9.5 s 通过；gemini-3-flash-preview 17–19 s 但两次三次违规（默认调色板类 / `data-href="#"`）；gemini-3.1-pro-preview 单屏 1–3 min 且写 arbitrary value / 外链违规，不采用。lint 同步放宽：`data-href="#"` 与 `href="#"` 同等视为未设计的死链接、不算违规（`form action="#"` 仍违规）。规则与 lint 仍按 claude-sonnet-5 调校，换模型后通过率以 `TC-CORE-005` 为准。2026-09-15 补充：gemini 驱动支持 Vertex AI 计费路径（`GEMINI_VERTEX=1`，鉴权走 ADC / 服务账号，区域 `global`，`us-central1` 无 3.x 模型）；「并行请求被排队」最终定位为 **Node ≥ 24 内置 fetch 默认 HTTP/2**：并发长请求复用同一条到 googleapis 的连接后被逐个处理（与 Vertex / 配额 / 代理无关，AI Studio 路径同样受影响）；修复：API 与 worker 入口首行加载 `lib/http-agent.ts`（`setGlobalDispatcher(new Agent({ allowH2: false }))`），6 屏从 118–247 s 降到 74 s（`RUN-026`）。Vertex 默认模型定为 gemini-3.7-flash（容量充足、零 429），3.8-flash 在 Vertex 上时有 `RESOURCE_EXHAUSTED` | @bug |
| 2026-09-14 | v0.7 | 验收缺陷两项（`REQ-PROTO-001`）：① 没有对应屏的导航写 `href="#"`、点击只提示不跳转（契约不再允许指向无关屏；ADR-003 补充）；② 聚焦屏内触控板捏合转发给画布缩放，画布外捏合屏蔽浏览器整页缩放。§19 增 2 个场景。开发工具：`pnpm seed` 只清测试账号数据 | @bug |
| 2026-09-11 | v0.6 | 验收反馈三项：① `REQ-PROTO-001` 导航源扩为 `<a href>` / `<form action>` / `[data-href]`（运行时与导出同构劫持，ADR-003 补充），契约与 lint 增 `form-action` 规则（ADR-005）；② `REQ-PROTO-002` 画布连线可视化（可开关）+「补链」修复轮（ADR-008 补充）；③ `REQ-EDIT-001` / `API-EDIT-001` 增 `link` 操作（检查器手动连线，零 token）。§19 增 4 个场景。实现期补充：经 `API-CORE-006` 直发的 `edit_screens`（补链）同样写对话记录，且前端发起任何直发作业后立即刷新对话面板（v0.5 ④ 的延伸）。生成中卡片的骨架占位改为线框 + 光带（纯视觉，无契约变化） | @bug |
| 2026-09-10 | v0.5 | M2–M5 实现完成（`REQ-PROTO-001`~`004`、`REQ-EDIT-001`~`003`、`REQ-AGENT-001`~`005`）并按 `docs/TEST.md` 执行；实现偏差回写：① `quilt.create_screen/update_screen` 同步执行（不经 `ingest_screen` 作业，§8 表更新）；② §7 新增 `ENT-OAuthClient`、`ENT-OAuthCode`、`ENT-DeviceToken`（OAuth DCR 与授权码、伴侣设备 token）；③ `API-AGENT-008` 响应增加 `onceToken` 字段；④ 经 `API-CORE-006` 直接创建的作业也写入对话记录（回刷/导出/局部重生成/懒生成有可见反馈）；⑤ 截图改由 `screenshot.render` 队列（pg-boss）承接（§17 已有）；⑥ 元素直改的 `style` 操作按契约一律被 lint 拒绝（422），token 类改动走 `classes` 操作 | @bug |
| 2026-09-10 | v0.4 | M1 实现完成（`REQ-CORE-001`~`010`，monorepo `apps/api` + `apps/web` + `packages/core`）并按 `docs/TEST.md` 执行 4 轮（AI 侧全部通过，`TC-CORE-020` 待人工）；实现与设计一致，无偏差；随聚焦交互一并实现了 `REQ-PROTO-001` 的链接劫持换屏 + 后退骨架与 `generate_missing_screen` 作业类型（UI 入口留到 M2）；ADR-004 补预览响应 CORS 说明；§23 检查单待 M1 上线前填写 | @bug |

## TEST.md 用例变更记录（按设计文档版本降序；无版本号的在末尾）

| 日期 | 改动 | 作者 |
| --- | --- | --- |
| 2026-09-22 | 设计文档 v0.55（组件预览里链接惰性）：`TC-EDIT-012` 第 9 步增断言——往交互态的组件里塞一个 `href="#"` 链接点下去，不得出现 toast、iframe 不得跳走；`Esc` 在焦点落于组件 iframe 内时仍能退出 | @bug |
| 2026-09-22 | 设计文档 v0.54（组件交互契约）：`TC-EDIT-012` 增第 10 步——组件要真能切，验收点落在「没有任何一条把选中样式硬编码进 class」与「点一下状态真的转移且容器不顶高」两条可量的断言上，而不是看截图像不像。组件禁 `<script>` 不变，所以这一步同时守住「交互是 CSS-only 实现的」 | @bug |
| 2026-09-22 | 设计文档 v0.53（画布位置留存 / 组件卡可交互 / e2e 认库闸）：`TC-CORE-007` 增第 7 步（刷新后首帧即记忆位置、逐帧只一个 transform、存储被改坏时回默认）；`TC-EDIT-012` 增第 9 步（双击组件卡进交互、镜头不动、与屏聚焦互斥、`Esc` 出）；§3 写明认库闸与 3200/3201 另起一套的跑法 | @bug |
| 2026-09-22 | 设计文档 v0.52（一键排成一行 / 排成一列，`REQ-CORE-018` 补充）：`TC-CORE-034` 第 2 步按钮数改为 10、增第 8b～8d 步——三屏叠在一处时点「排成一列」按 844 + 80 纵向排开、点「排成一行」按 390 + 80 横向排开、`⌘Z` 回到一列并 toast「已撤销排列」；第 9 步 `End` 落点改为 `arrange-vcol` | @bug |
| 2026-09-21 | 设计文档 v0.51（MCP 与画布同面，`REQ-AGENT-002` 补充）：新增 `TC-AGENT-011`——用纯 MCP / REST 脚本 `tests/e2e/mcp.ts`（`pnpm --filter @quilt/tests e2e:mcp`）把 32 个新工具与 4 处签名改动逐个过一遍（删 / 修订 / 候选 / 摆放 / 直改 / 六种作业 / 素材 / 预设 / 批注 / 项目），作业类步骤在 `LLM_DRIVER=stub` 下只验作业输入与终态；`TC-AGENT-001` 第 1 步的工具清单仍成立（子集）。脚本可经 `QUILT_E2E_API` 指到独立端口的 API，不占用 3100 的开发实例（§3） | @bug |
| 2026-09-21 | 设计文档 v0.50（输入框工具条几何）：`TC-CORE-023` 第 12 步——控件两两不相交、宽度按工具条量、切模式时宽度单调过渡而高度恒定、装不下时 `data-bar="wrap"` 仍全部可见。这四条都不是「有没有横向滚动」能查出来的：内容是溢出到兄弟节点身上、不撑文档，高度抖动只在动画中途出现 | @bug |
| 2026-09-21 | 设计文档 v0.49（预览响应不再声明 immutable，`API-CORE-016` 修订）：`TC-CORE-010` 增第 3c 步——预览响应头为 `no-cache`，退出交互再进一次仍走真实网络请求。这一步守的是「运行时修复当场生效」这条承诺：v0.48 的修复推上去之后现场仍看得到滚动条，就是被 `immutable` 挡住的 | @bug |
| 2026-09-21 | 设计文档 v0.48（预览里不再露出滚动条，`ADR-003` 补充）：`TC-CORE-010` 增第 3b 步——聚焦屏的滚动根 `scrollbar-width` 为 `none`、槽宽 0，但 `scrollHeight > clientHeight` 且滚轮滚得动（视觉无条、实质可滚）。无头浏览器默认 overlay 滚动条、槽宽恒 0，这一步必须在有头 Edge 里量才有区分力 | @bug |
| 2026-09-21 | 设计文档 v0.47（多选批量移动，`REQ-CORE-004` 补充）：`TC-CORE-007` 前置增建一个共享组件，增第 4～6 步——加选 2 屏 + 组件后按住其中一屏拖动整组同位移、未选中的屏不动、屏与组件各自 PATCH 落库；按在已选卡片上不拖则收成只选它；`⌘Z` 整组还原并 toast「已撤销移动」 | @bug |
| 2026-09-20 | 设计文档 v0.46（共享组件 `REQ-EDIT-006`）：新增 `TC-EDIT-012`（从屏里提取导航栏为组件并同步到其他屏、`PATCH` 改组件后三屏确定性回刷、版本冲突 409、实例内直改 409 component-locked 与 `detach` 脱离、改名跟随、删组件屏不动、画布组件卡 / 目标标签 / 检查器锁定提示 / 新建组件、真实 `edit_component` 回合）；§3 注明 fixture 的 `<nav>` / `<header>` 是提取靶子 | @bug |
| 2026-09-20 | 设计文档 v0.45（聊天模式 `REQ-CORE-023`）：新增 `TC-CORE-039`（非 Claude 通道 / 没有 agent-sdk 通道 400、在跑 chat 作业 409、只问不改的回合不动屏、跨轮指代改一屏且修订记在聊天作业名下、段控切聊天后档位隐藏 / 动词行 / 通道只列本机 Claude 订阅 / 在跑作业行与回执 / 刷新记住模式）；脚本 `tests/e2e/chat.ts`；§3 补本机 claude 登录态前置。 | @bug |
| 2026-09-20 | 设计文档 v0.44（字体来源）：新增 `TC-EDIT-011`（Google 任意族名进 prelude 链接、本机字体不发外链且栈以族名开头带 `-apple-system` 回退、自定义样式表链接原样进 `<link>`、来源为 url 缺链接与带引号族名 400、面板回显 / 就地报错不提交 / 切来源收起链接框）；`tests/e2e/edit.ts` 同步。 | @bug |
| 2026-09-20 | 设计文档 v0.43（设计契约从闸门改成透镜）：三处断言翻转——`TC-EDIT-002`（直改引入内联样式）、`TC-PROTO-007`（表单去掉 action）、`TC-AGENT-004`（MCP 推违规 HTML）由「422 lint-failed、不落库」改为「照常落修订 / 建屏，偏离进 `lintReport` 并在卡片上报「N 处偏离」」；新增外部库用例（推一张引 Chart.js 的屏，预览域里真的画出来）。 |
| 2026-09-20 | 设计文档 v0.42（MCP 补两个写入口）：`TC-AGENT-002`（MCP 工具面）增两步——`quilt.update_design_system` 带 `expectedVersion` 改色板并 `applyToScreens` 回刷（屏的 prelude 换色、body 不动、版本过期回 `409`），`quilt.update_project` 改名与 brief。 |
| 2026-09-20 | 设计文档 v0.41（画布位置可撤销）：`TC-CORE-034` 增第 8 步——对齐后按 `⌘Z` 位置回到对齐前并 toast「已撤销对齐」，再按一次提示「没有可撤销的移动」。 |
| 2026-09-20 | 设计文档 v0.40（项目重命名 + 设计预设）：新增 `TC-CORE-037`（切换器行内重命名：hover 露出、就地编辑、回车存、`Esc` 放弃、空名不改）与 `TC-CORE-038`（设计预设：连素材存 → 新建项目按预设开局 → 已有项目套用并问回刷 → 删预设不影响已套用的项目）。 |
| 2026-09-19 | 设计文档 v0.39（v0.38 收尾）：`TC-EDIT-005` 第 2 步改为「只改正文不弹回刷、焦点兜底落到『回刷所有屏』」；`tests/e2e/edit.ts` 点弹层里的 `ds-apply-confirm`（旧脚本点面板那颗同名键会被 inert 挡住必超时）、`tests/e2e/agent.ts` 的「已投递」改等待式（面板改轮询后最长 1.5s 才翻牌）。 |
| 2026-09-19 | 设计文档 v0.38（八组验收缺陷回写）：`TC-CORE-029` 第 6 步改为「先摆好目标与草稿再让后台造屏」并增第 6b 步（收口后目标标签与草稿都不变、进度兜底写「排队中…」）；`TC-CORE-030` 第 3 步增「展开期间排列条不在场」、新增第 3b 步（整组采用被逐屏跳过时报跳过、零采用不收起）、第 4 步删掉与现状不符的「Esc 关闭」（采用后自动收起）并补「排列条随之回来」；`TC-CORE-034` 增第 8~9 步（1024 窄视口下排列条避让右侧面板且按钮可点、48rem 以下面板开着时整条让位；工具条键盘模式方向键 / `Home` / `End` 环绕 + 一次 `Tab` 离开）；`TC-CORE-035` 增第 3b、3c、7b 步（改名文件按正文魔数认类型、坏 / 越界 `viewBox` 不再 500、并发 60 次上传仍卡在 50 个、素材区拖拽上传与非图片就地说明）并在第 7 步写明展示 `<img>` 不带 `crossOrigin`；`TC-CORE-036` 第 1 步把「带进度」明确为兜底文案「排队中…」；`TC-EDIT-005` 第 1~2 步改为「未保存时回刷不可点 → 保存后弹 `ds-apply-ask` → 点 `ds-apply-confirm` 才建回刷作业」；`TC-EDIT-010` 第 5 步增暗色方案断言（`surfaceVariant` 取暗值、对比度 ≥ 4.5:1、语义色切 80/20）、新增第 5b 步（撤掉暗色色板自动回落 `light`、`dark:{}` 等同没有暗色色板、显式选 `dark` 仍 400）与第 8 步（存量行删掉语义键后重启即回填、`version` 不变、幂等）；`TC-AGENT-009` 第 3 步增「面板开着时活跃 `EventSource` 仍为 1、后台标签页不轮询」、第 5 步的「已投递」改为等待式断言（面板改轮询后最长 1.5 s 才翻牌，一次性断言会变脆）。`tests/e2e/core.ts` 同步：补三条最值得防回归的断言——`TC-CORE-029` 的「后台造屏不顶目标与草稿」、`TC-CORE-030` 的「展开期间排列条不在场」与「全部被跳过时说明跳过且不收起」；候选角标选择器改为 `[data-testid="candidate-badge"][data-route=…]`（角标已移出卡片，后代选择器失效），`TC-CORE-032` 保存后改点 `ds-apply-confirm`。`TC-CORE-034` 第 8~9 步（窄视口避让、工具条键盘模式）与 `TC-CORE-035` / `TC-EDIT-010` 的新步骤本轮只入文档、脚本未覆盖，按人工或下一轮补 | @bug |
| 2026-09-19 | 设计文档 v0.37（一个标签页只开一条长连接）：`TC-CORE-036` 增第 10 步——两个作业同时在跑时，该标签页的活跃 `EventSource` 恒为 1（作业进度改走项目事件流的 `job_changed` 投影）。多开标签页的连接额度是浏览器全局 6 条，实测第 6 条起普通请求即超时，这一步是它的回归闸。 |
| 2026-09-19 | 设计文档 v0.36（并行作业 `REQ-CORE-020`）：新增 `TC-CORE-036`（两个作业同时在跑时输入框仍可输入、在跑作业逐行呈现并各带取消键、只有这一轮真冲突才挡住发送、改另一张空闲屏放行且两个作业各自落地、`Esc` 取消最新、逐行取消只取消那一个）；`TC-CORE-013` 第 2 步与 `TC-CORE-023` 第 10 步改写——此前把「有作业在跑时输入框禁用」「占位文案提示按 `Esc` 可取消」当验收点 | @bug |
| 2026-09-19 | 设计文档 v0.35（品牌色板 + 项目素材库）：新增 `TC-EDIT-010`（品牌色板：语义色恒在、逐键覆盖、回刷只换 prelude、无暗色色板拒切、清空回派生、面板 26 键）与 `TC-CORE-035`（项目素材库：SVG 取 viewBox 尺寸、URL 不签名可取且长缓存、非图片 422、素材清单进 system 前缀、详情带 `assets[]`、风格指南卡片画出素材、面板确认后删除）；`openai-stub` 记下 system 前缀供断言。验收反馈后 `TC-CORE-035` 补第 5～6 步，`TC-CORE-023` 的画布空态不再有「屏幕会生成到这里」那句文案。 |
| 2026-09-19 | 设计文档 v0.34（本机 agent 改为投递到活跃 Claude Code 会话）：`TC-AGENT-009` 重写为假会话（登记文件 + socket）验「列表命名规则 / 会话下拉 / 投递提示词与回执 / 屏锁 / 取消后 finish_job 409 / failed · 旧基线 · succeeded 三种收口 / 选择记忆与失效」；`TC-AGENT-010` 改为人工（投递到真实会话要在终端确认）；`TC-CORE-025` 第 7 步改为「缺 / 死 sessionId 建作业前 400」；§3 的 `QUILT_AGENT_COMMAND` 换成 `QUILT_CLAUDE_SESSIONS_DIR`；§7 遗留改写；同轮验收补 `TC-CORE-010` 第 5b 步（焦点在 iframe 里按 `⌘E` 由运行时转发）、`TC-EDIT-003` 第 1a 步（检查器通道单独记忆、作业带该 runner）与第 1~3 步的屏内标记 / 根沿用 qid 断言、`TC-AGENT-009` 第 8 步的「不操作即热更新」断言（项目级事件流 `API-CORE-030`）；`TC-CORE-028` 第 7d 步（预置通道按账号移除 / 恢复）随该功能删除，第 9 步去掉「已移除项不进下拉」；新增 `TC-CORE-033`（删项目：hover 垃圾桶 / Delete 键 → 确认 → 级联与对象清理、有作业 409、删当前项目回首页；标签页标题带项目名）；新增 `TC-CORE-034`（多选排列：六种对齐 + 两种等距，经既有位置 PATCH 落库，不足 3 屏等距不可用）；候选覆盖层改为画布就地展开后 `TC-CORE-029` 第 5 步、`TC-CORE-030` 第 3 步改写（`candidate-stack` / `cand-ghost` / 每格活 iframe / 采用后自动收起）；去掉「系统预置」后 `TC-CORE-025` 第 1 步改为「云端通道 = 自己配的（seed 按 `.env` 建一条 Gemini）」、`TC-CORE-028` 第 7 步改两组、删第 7c 步 | @bug |
| 2026-09-18 | 设计文档 v0.33（输入框可收起 `⌘/` + 聚焦态热更新）：`TC-CORE-023` 增第 8b~8d 步（`⌘/` 收起 / 叫回并保草稿、工具栏同一开关、聚焦自动收起 / 退出恢复）；新增 `TC-CORE-032`（聚焦态热更新：桩通道改屏后 iframe 原地更新、不重挂、滚动位置保持；栈顶屏回刷后原地换色、后退到栈底屏也是新修订）；`TC-EDIT-001` 第 2 步增「iframe 不重挂」；`TC-EDIT-003` 增「聚焦不退出即见子树更新」；桩 `openai-stub` 复用 | @bug |
| 2026-09-18 | 设计文档 v0.32（本地单用户版 M10，`ADR-016`）：去账号——`TC-CORE-001` / `002` / `021` 随 `REQ-CORE-001` 推迟，全部用例的「以 Owner 登录」「会话 cookie」「`--owner`」前置移除，§3 改为单用户 + 测试库 `quilt_test` + 两种运行形态；去硬上限——`TC-CORE-016` 重写为「台账无上限仍放行 + 在途预估可见」、`TC-CORE-022` 重写为「设置弹层汇总 / 按驱动分列 / MCP 接入命令」；`TC-CORE-018` 第 3 步改为新标签页重开；`TC-CORE-025` 第 7 步改为「本机 agent 建 runner=agent 作业、列入 `GET …/jobs?runner=agent`、CLI 不在 PATH 时 400 并给安装步骤」；`TC-CORE-026` 第 1、2 步改在 PAGE-FIRST（`pnpm seed --empty`）量品牌；`TC-CORE-028` 第 7 步左栏改为 2 节；本机 agent 改由 Quilt 进程拉起 CLI——`TC-AGENT-001` 重写为免鉴权接入、`TC-AGENT-002` / `005` / `006` / `007` / `008` 推迟、`TC-AGENT-009` 重写为桩 CLI 验拉起 / 屏锁 / 回写守卫 / 退出码 / 取消、新增 `TC-AGENT-010`（真实 Claude Code 跑一次）；新增 `TC-CORE-031`（`npx quilt-canvas` 冷启动，脚本 `tests/e2e/install.ts`）；种子 `seed` 增 `--empty`、`seed:job` 增 `--screens`，`seed:login` / `seed:magic-link` / `seed:quota` / `seed:task` / `seed:device` / `mcp:grant` / `mcp:await` / `task:lease-expire` 删除 | @bug |
| 2026-09-18 | 设计文档 v0.31（动词版 M9）：`TC-CORE-029` 重写为「双击放锚点 → 输入框造 1 屏 × 3 版 → 候选修订 → 覆盖层采用 → 修订面板折组 → 造一组按流程排一行并反向连线」；新增 `TC-CORE-030`（⌘A × 2 版整组采用）、`TC-EDIT-009`（⌘A 不改设计系统；记为约定 → 提炼 → 预览 → 写入约定节 → 面板可删）、`TC-AGENT-009`（agent 作业映射：租约过期释放屏锁、重领重快照、旧基线 409、缺 expectedRevisionId 400、完成回执）；`TC-CORE-016` 增在途预估计入余额三步；`TC-CORE-024` 增第 5b 步（目标标签不随点空白清除、清空回到造）；`TC-CORE-025` 第 7 步改为「建 runner=agent 作业 + 挂 jobId 的任务、不占输入框」；`TC-CORE-014` 面板断言按修订树（每条标派生自哪一版）；作业种类 `generate_screens` / `generate_screen` / `generate_missing_screen` 在用例与种子里统一为 `generate`；种子 `seed:job` 增 `--input`、`seed:task` 增 `--job` 模式 | @bug |
| 2026-09-18 | 设计文档 v0.30（通道下拉只给图标 + 显示名 / 画布顶栏去品牌 / 分段控件同心圆角）：`TC-CORE-025` 第 3 步增「模型 id 不进下拉」，`TC-CORE-028` 第 7 步增「管理器行里写出模型 id」，`TC-CORE-026` 第 2 步改为在登录页量字标并断言画布顶栏无字标 | @bug |
| 2026-09-17 | 设计文档 v0.29（画布指针 / 设置弹层分栏 / 多屏生成改为独立屏）：`TC-CORE-007` 第 1、2 步增指针断言；`TC-CORE-018` 第 3 步、`TC-CORE-022` 第 2 步、`TC-CORE-028` 第 7 步改为分节 URL 与左栏 tab 操作，028 第 7 步增「方向键换节、URL 跟随」；`TC-CORE-029` 重写为「N 张独立屏并排、留一删余、无链接规划的出屏提示写明暂不跳转」 | @bug |
| 2026-09-17 | 设计文档 v0.28 导航模型（画布即主界面：`/` 直接进最近项目，项目列表页改为顶栏项目切换器，设置页改为 `?settings=1` 设置弹层）：`TC-CORE-001` 第 1、3、4 步、`TC-CORE-003` 第 1 步、`TC-CORE-018` 第 3 步、`TC-CORE-022` 第 2 步、`TC-CORE-028` 第 7~9 步改为经切换器 / 弹层操作；`TC-CORE-028` 第 8 步增「内层关闭后设置弹层仍在、背景仍 inert」 | @bug |
| 2026-09-17 | 设计文档 v0.28（画布空白处凭空生成一屏 + 多版候选 `REQ-CORE-014`）：新增 `TC-CORE-029`（三个入口开浮框、3 版候选不入主链与地图、选定后转 current 且重复选 409、count=1 直接 current 与路由去重） | @bug |
| 2026-09-17 | 设计文档 v0.27（预置 / 本机通道可按账号移除、可恢复）：`TC-CORE-028` 增第 7d 步，第 9 步增「已移除项不进下拉」 | @bug |
| 2026-09-17 | 设计文档 v0.25（生成通道可配置）：新增 `TC-CORE-028`（通道增删改验、密钥零泄漏、作业按 channel 形态跑通、设置页管理器与输入框下拉只列可用项）；`TC-CORE-025` 步骤 2~3 的「下拉列出全部通道且不可用项灰化」改为「下拉只列可用项，不可用项在设置页看」 | @bug |
| 2026-09-17 | 设计文档 v0.24（本机三条通道传图）：`TC-CORE-027` 第 3、6 步改为条件步骤——当前所有通道 `vision=true`，护栏路径无通道可触发，脚本按清单自动跳过 | @bug |
| 2026-09-17 | 设计文档 v0.23（参考图输入）：新增 `TC-CORE-027`（附件类型/大小校验、无视觉通道的双重护栏、贴图上限与移除、`attachmentIds` → `imageKeys` 贯通） | @bug |
| 2026-09-17 | 设计文档 v0.22（风格指南卡改报项目字体）：`TC-CORE-026` 第 5 步由「卡上不出现 Space Grotesk」改为「卡上报出项目自己的字体、且不等于工具外壳字体」，并加一条防止两者取同一字体时断言失去区分力的守卫 | @bug |
| 2026-09-17 | 设计文档 v0.21（新建项目不问种子色）：`TC-CORE-003` 第 1~2 步改为断言弹窗只有 1 个可填字段、无取色器与「种子色」字样，且服务端在未传时回落到默认种子色 | @bug |
| 2026-09-16 | 设计文档 v0.20（输入框不随侧边浮层平移）：`TC-CORE-023` 第 9 步改为断言折叠/展开不移动输入框，新增第 9b 步（右侧面板让位但不过度退让、输入框压在视口正中） | @bug |
| 2026-09-16 | 设计文档 v0.19（品牌接入）：新增 `TC-CORE-026`（品牌标识、字体、深底对比度与「用户设计稿不带工具品牌」的边界） | @bug |
| 2026-09-16 | 设计文档 v0.18（输入框按参考件重做，通道下拉改为自定义列表）：`TC-CORE-025` 原步骤 2 拆为 2~5——触发器默认值与名称、列表分组与不可用项就地说明、`Esc` 只关列表不清画布选中、选择后跨刷新记忆 | @bug |
| 2026-09-16 | 设计文档 v0.13/v0.14（M6）：新增 `TC-EDIT-008`（元素批注攒批发送）、`TC-CORE-025`（生成通道可选）；`TC-EDIT-001`/`TC-EDIT-007` 按「选择元素可直接进入、与交互态互斥」更新步骤 | @bug |
| 2026-09-14 | 设计文档 v0.7：新增 TC-PROTO-010（未设计交互只提示）、TC-PROTO-011（聚焦屏内捏合缩放）；`--form` fixture 增 `href="#"` 链接；`pnpm seed` 改为只清测试账号数据（§3） | @bug |
| 2026-09-11 | 设计文档 v0.6：新增 TC-PROTO-007（表单提交 / data-href 跳转）、TC-PROTO-008（画布连线开关）、TC-PROTO-009（补链修复轮）、TC-EDIT-007（检查器手动连线 + form-action 规则）；种子 `seed:project` 增 `--form`、`--unlinked` | @bug |
| 2026-09-09 | 初稿：TC-CORE-001~022、TC-PROTO-001~006、TC-EDIT-001~006、TC-AGENT-001~008，覆盖 REQ-CORE-001~010、REQ-PROTO-001~004、REQ-EDIT-001~003、REQ-AGENT-001~005 | @bug |
| 2026-09-10 | §3 环境按 M1 实现修正并冷启动实测通过（端口 3100/3101、`docker compose` Postgres、fs 对象存储、outbox 邮件、`.env` 来源、curl 需 `--resolve`）；无用例增改；AI 执行脚本 `tests/e2e/core.ts`（RUN=轮次、ONLY=用例子集）与 `tests/e2e/core-stub.ts`（TC-CORE-006） | @bug |
